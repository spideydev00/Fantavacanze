import 'package:flutter/material.dart';

class FvBotPage extends StatelessWidget {
  const FvBotPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Fv Bot'),
      ),
      body: Center(
        child: Text(
          'Fv Bot Page',
        ),
      ),
    );
  }
}
